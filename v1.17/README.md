# v1.17

Хотфикс сборки:
- исправлен `normalizeTeacherName`, где Java ломалась на Unicode-символах в `.replace(...)`;
- NBSP/тире заменены на безопасные `(char) 160`, `(char) 8211`, `(char) 8212`;
- файл записан в UTF-8 без BOM;
- AndroidX/FileProvider убраны, чтобы не было ошибки `checkDebugAarMetadata`.

Новая функция:
- добавлен раздел «Оценки и посещаемость»;
- вход в dekanat.bsuedu.ru через WebView;
- пароль не сохраняется;
- после входа открывается журнал СПО;
- можно выбрать семестр/неделю и попробовать собрать оценки/Н/НН.

Известные баги:
- это первая версия функции оценок, парсер может потребовать доработки после теста на реальной таблице.

Версия:
- versionCode = 43
- versionName = 1.17
