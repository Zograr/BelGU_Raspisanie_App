# Расписание БелГУ — версия с выбором подгруппы

Приложение показывает расписание на сегодня для группы 90002595.

Что изменено:
- добавлен выбор подгруппы: 1, 2 или все;
- выбранная подгруппа сохраняется после закрытия приложения;
- в карточках остаются только пары выбранной подгруппы;
- лекции и общие занятия показываются для всех;
- сохранение последней загруженной версии.

## Как собрать APK

1. Открой проект в Android Studio.
2. Дождись Gradle Sync.
3. Нажми `Build -> Generate App Bundles or APKs -> Generate APKs`.

Либо через Terminal:

```bat
.\gradlew.bat assembleDebug
```

Готовый APK будет здесь:

```text
app\build\outputs\apk\debug\app-debug.apk
```
