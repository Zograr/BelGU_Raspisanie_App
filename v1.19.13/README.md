# v1.19.13

Фиксы после возврата старой системы оценок:

- исправлен выбор семестра/недели:
  - приложение больше не должно открывать выбор языка RU/ENG при быстром нажатии;
  - списки языка теперь игнорируются.
- убрана строка «Всего записей: 300», потому что она вводила в заблуждение.
- полностью убрана галочка «Не выходить из системы» и связанная логика.
- исправлена кнопка «Открыть сайт вручную»:
  - теперь она открывает обычный видимый сайт;
  - не запускает вход с сохранённым паролем.
- убрана отдельная кнопка «Показать 2 / Н / НН».
- кнопка «Показать проблемы: 2 / Н / НН» оставлена в результатах после сбора оценок.

Версия:
- versionCode = 81
- versionName = 1.19.13

Известные баги:
- интерфейс оценок использует старую рабочую систему из v1.17.14, поэтому визуально может отличаться от новых экранов.


## Build fix 2
- Полностью заменён участок от `showGradesPanel()` до `hideGradesPanel()`.
- Убран оставшийся «хвост» кода вне метода, из-за которого Java ругалась на `refreshGradesPanelContent()`.
